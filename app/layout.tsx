import type React from "react"
import type { Metadata } from "next"
import { Inter, JetBrains_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import DNABackground from "./DNABackground" // Import the DNABackground component

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
})

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
})

export const metadata: Metadata = {
  title: "DNA-Lang | Quantum-Biological Computing Platform",
  description:
    "Revolutionary web platform for designing, visualizing, and evolving self-aware quantum organisms using DNA-Lang and Integrated Information Theory",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}>
        <DNABackground />
        {children}
        <Analytics />
      </body>
    </html>
  )
}
